import NewClientSubmission
  from '@/elements/components/webhook-clients/new-client-submission';

const CreateClient = () => { 
  return (
    <NewClientSubmission />
  )
}

CreateClient.currentPage = 'Create';


export default CreateClient;